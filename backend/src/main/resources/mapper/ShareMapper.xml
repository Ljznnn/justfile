<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.justfile.mapper.ShareMapper">

    <select id="selectByShareCode" resultType="com.justfile.entity.Share">
        SELECT * FROM share WHERE share_code = #{shareCode}
    </select>

</mapper>